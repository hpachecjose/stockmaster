# Configurações do Banco de Dados
DB_HOST=postgres
DB_PORT=5432
DB_USER=stockmaster
DB_PASSWORD=stockmaster
DB_NAME=stockmaster_db

# Configurações do Redis
REDIS_URL=redis://:stockmaster_redis@redis:6379

# Autenticação (JWT)
# OBRIGATÓRIO — gere com: openssl rand -hex 64
JWT_SECRET=
